# Independent random variable

# Definition

:::definition
    Let $X$ be a real random variable. We say that $(X_n)_{n \in \mathbb{N}} is a sequence of independent random variable if the under $\sigma$-algebra is independent in is set.
:::

# Result